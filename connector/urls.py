from django.conf.urls import url
from . import views

urlpatterns = [
    #url(r'^connector/$', views.connector, name='connector'),
    #url(r'^create_connector/$', views.create_connector, name='create_connector'),
    #url(r'^search/$', views.search, name='search'),  
]
